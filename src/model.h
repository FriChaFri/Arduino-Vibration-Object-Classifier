/*
 * model.h
 * Tiny classifier or neural network inference.
 */

#pragma once
#include "features.h"

inline int classify(const FeatureVector &f) {
    // TODO: replace with NN or trained classifier
    // Return class index
    return 0;
}
